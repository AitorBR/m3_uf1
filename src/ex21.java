import java.util.Scanner;

public class ex21 {
    public static void main(String[] args) {
        /*Scanner poner = new Scanner(System.in);
        String x, inv = "";

        int pos;

        System.out.println("Escribe una palabra");
        x = poner.nextLine();

        pos = x.length();

        for (int cont = pos; cont > 0; cont--) {
            if (inv.length() != 0) {
                inv = inv + x.charAt(cont);
            } else {
                inv = x.charAt(cont);
            }
            System.out.println(inv);
        }
        if (x.equalsIgnoreCase(inv)) {
            System.out.println("es cap y cua");
        } else {
            System.out.println("no es cap y cua");
        }*/
    }
}